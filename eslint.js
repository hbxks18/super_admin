module.exports = {
  extends: ["airbnb", "sf-js", "sf-react"],
  rules: {
    // Allow unused vars when you are debuging, but can't build.
    // "no-unused-vars": process.env.NODE_ENV === "development" ? "warn" : "error",
  },
}
